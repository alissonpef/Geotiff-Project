# Server configuration
PORT=3001

# Data directory (where .tif files are located)
DATA_DIR=./data

# GeoTIFF cache settings (in minutes)
CACHE_AGE_MINUTES=60

# CORS - allowed origin for cross-origin requests
CORS_ORIGIN=*
